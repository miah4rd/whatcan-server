---
name: "fb-rental-demand-scout"
description: "Слотовый прогон FB: запросы арендаторов → карточки FB Lead в amoCRM Rental. ЗАКОН №1 — прогон не закончен, пока в amoCRM не заведён минимум 1 новый лид С НОМЕРОМ WHATSAPP; единственное основание остановиться — подтверждённый lead_id, а объяснение, чек-лист и отчёт результатом не являются (см. fb-rental-demand-scout-gate). ЗАКОН №2 — один человек постит в 3-5 групп, дубль в CRM недопустим. ЗАКОН №3 — исходящее не длиннее 600 знаков и ровно одна зацепка. Шаг 1 включает ДВА инбокса: личный Messenger и Page Inbox бизнес-страницы Monthly/Yearly Properties в Meta Business Suite."
---

# FB Lead Scout — слотовый прогон запросов арендаторов

Цель: найти в Facebook свежие посты людей, которые ИЩУТ виллу/дом в аренду на Бали, получить их WhatsApp и завести карточки в amoCRM. Это сторона СПРОСА (арендаторы), не предложения (владельцы/виллы).

Прогон запускается по слотам каждые 2 часа: 10:00, 12:00, 14:00, 16:00, 18:00 WITA.

**Читать вместе с `fb-rental-demand-scout-gate`.** Гейт старше этого файла во всём, что касается условий остановки прогона.

Работа идёт **во встроенном браузере Claude** через инструменты `mcp__Claude_Browser__*`. С 27.08 это единственный рабочий путь — `mcp__claude-in-chrome__*` больше не использовать. Во встроенном браузере уже залогинены оба нужных аккаунта: Facebook профиль 61560547922812 (Nikita Irbitskii, представляется как «Nick from Unicorn Property») и amoCRM https://unicornproperty.amocrm.ru (аккаунт Unicorn-property, id 31830854). В начале прогона проверять авторизацию: на вкладке facebook.com `(document.cookie.match(/c_user=(\d+)/)||[])[1]` должно вернуть `61560547922812`, на вкладке unicornproperty.amocrm.ru `GET /api/v4/account` должен вернуть 200.

Технические особенности встроенного браузера — известные факты, не диагностировать их заново каждый прогон:
- В `javascript_tool` **нет top-level await**. Писать через `.then()`. Конструкция `await fetch(...)` падает с SyntaxError.
- Возвращать из JS **только нужные поля собранной строкой**, а не сырой JSON или `res.text()`: сырой вывод режется фильтром с сообщением `[BLOCKED: Cookie/query string data]`.
- amoCRM читать/писать через `fetch(url,{credentials:'include'})` из вкладки на домене `unicornproperty.amocrm.ru`. На `www.amocrm.ru` другой origin и сессии там нет.
- Основной способ чтения страниц — `get_page_text` / `read_page`. `computer{action:"screenshot"}` — когда нужна дата поста или проверка баннера лимита.
- Поиск по группе отдаёт ~5 результатов по релевантности и **не догружается скроллом**. Чтобы достать больше — менять формулировку запроса, а не листать выдачу.
- Лента группы виртуализирована: `innerText` отдаёт 1-2 поста независимо от скролла. Для ленты работать скриншотами либо целевыми запросами.
- Отправка сообщения в Messenger: сфокусировать композер через `document.querySelector('div[contenteditable="true"][aria-label="Write to <Имя>"]').focus()`, напечатать текст, затем нажать синюю кнопку отправки по координатам. Enter в композере не отправляет. Перед отправкой проверить поле «To:» — там должен быть ровно один получатель; случайный ввод в поле получателей добавляет лишних людей в тред.

## КВОТА — главное правило

- **1 исходящее в новый или холодный тред за один прогон. Не больше.**
- **5 исходящих за скользящие 24 часа.** Пять слотов по одному и есть дневная норма.
- Окно **скользящее, а не календарное**: сообщения вчерашнего вечера продолжают занимать квоту сегодня утром. Считать всегда по времени последних 24 часов, а не с полуночи.
- Follow-up тратит ту же квоту, что и холодный DM. Гипотеза «follow-up бесплатный» проверена и не подтвердилась.
- **Ответ в уже открытый живой тред квоту не тратит и лимитом не бьётся.** Дожимать тех, кто ответил, можно свободно даже при блоке.
- Не больше 1 follow-up на одного человека. Человек, получивший DM и один follow-up, исчерпан — писать ему третий раз нельзя.
- Каждое сообщение переписывается под конкретного человека. Copy-paste одного текста нескольким людям запрещён.
- Перед отправкой проверять лимит живьём: открыть `https://www.facebook.com/messages/t/<user_id>`, убедиться что поле ввода «Aa» активно и нет баннера. Если баннер «You've reached the message request limit» — лимит не обходить, зафиксировать дословно как артефакт внешнего отказа и остановить все исходящие до освобождения окна.
- Наш номер и ссылки wa.me не давать никому и нигде.
- Больше групп в списке не значит больше исходящих — квота не меняется. Расширенный список даёт (а) больше кандидатов с WhatsApp прямо в посте, которых можно заводить в CRM бесплатно без квоты, и (б) более широкий выбор для того единственного, кому пишем за слот.
- **Квота — потолок, а не план. Пустой слот по исходящему — допустимый исход.** Если нет кандидата, который проходит все проверки Шага 3, исходящее не отправляется вовсе. Заполнять слот пограничным кандидатом запрещено. Пустой слот по карточкам допустимым исходом НЕ является — см. гейт.

В отчёт каждого прогона писать состояние квоты в формате: `КВОТА: исходящих за прогон X из 1, за скользящие сутки Y из 5`.

## Шаг 1. Два инбокса

**1а. Личный Messenger.** https://www.facebook.com/messages/ , вкладка Unread и раздел Message requests (`/messages/requests/`). Делается всегда и первым — лимитом не ограничено. Список тредов слева показывает время последнего сообщения — по нему же считать израсходованную квоту за скользящие сутки.

**1б. Page Inbox** бизнес-страницы Monthly/Yearly Properties: https://business.facebook.com/latest/inbox/all — отдельный поток, в личном Messenger он не виден.

В обоих:
- Ответы на наши прошлые DM с номером WhatsApp → карточка (Шаг 4).
- Новые входящие от арендаторов → если нет номера, попросить WhatsApp; если есть — карточка.
- Игнорировать агентов и УК, предлагающих свои объекты: пишут «Super Offer», «I'm helping a villa owner», присылают листинги, либо спрашивают нас «Still looking for villa?».

## Шаг 2. Прогон групп

По каждой группе открывать `https://www.facebook.com/groups/<ID>/search/?q=looking%20for` и читать через `get_page_text`. Не тратить попытку на `javascript_tool` с селектором `div[role="article"]` — он не работает в текущей вёрстке FB, это известный факт.

Экономичный обход: `navigate` + `javascript_tool` складывать в один `browser_batch` по 2-3 группы за вызов, разворачивать «See more» и возвращать срез `document.body.innerText`.

Чтобы вытащить кандидатов с номером прямо из поста — прогонять по тексту страницы регулярку `/\+\d[\d\s\-().]{7,}|08\d{8,}|wa\.me\/\d+/g` и возвращать по 600-900 знаков контекста перед каждым совпадением. Это дешевле, чем читать выдачу целиком.

**Про даты и свежесть.** Выдача поиска ранжируется по релевантности, а НЕ по свежести — в первой пятёрке регулярно попадаются посты недельной давности. Переключатель «Recent posts» порядок не исправляет. В `get_page_text` даты чаще всего нечитаемы. **Рабочий способ:** `computer{action:"screenshot"}` страницы поиска — под именем автора дата видна открытым текстом. Дату финального кандидата обязательно проверять скриншотом перед отправкой сообщения. Если скриншот падает по таймауту «Browser pane is not displayed» — это временный сбой, повторить позже, отказом он не является. Дополнительные способы: целевой поиск по группе по уникальной фразе из текста поста; JS-фильтр по шаблонам меток времени.

Полный текст поста разворачивать одним вызовом:
`let n=0;[...document.querySelectorAll('div[role="button"],span')].forEach(e=>{if(e.innerText==='See more'){e.click();n++}});n`

user_id автора для Messenger:
`[...document.querySelectorAll('a[href*="/user/"]')].map(a=>a.innerText.trim()+' => '+a.href.split('?')[0]).filter(v=>v.length>4).filter((v,i,s)=>s.indexOf(v)===i).join('\n')`

**Важная техническая заметка про список групп аккаунта.** Страница `/groups/joins/` по умолчанию отдаёт только часть из 43 групп и не подгружает остальное скроллом. Чтобы увидеть остальные — переключать сортировку через кнопку «Sort» (Default / Alphabetically / Most visited / Recently joined first / Earliest joined first): каждая сортировка показывает свой срез.

Тир 1, проходить каждый слот (список не сокращать), в порядке приоритета:
- https://www.facebook.com/groups/balimonthlyvillas/ — Bali MONTHLY Rental Villas (93.9K) — лучший источник.
- 159972917747253 — CANGGU HOUSE, VILLA SHARES, MONTHLY RENT AND SITTING.
- 3234845689886542 — CANGGU PERERENAN UMALAS land villa & house RENT/SELL/BUY.
- 460364820648524 — BALI RENTAL ROOMS & VILLAS.
- 921979121570193 — Monthly Rent Villa & House Bali.
- 525784972516568 — SEMINYAK KEROBOKAN UMALAS BALI VILLAS AND APARTMENTS MONTHLY-YEARLY RENTALS.
- https://www.facebook.com/groups/balihomerent/ — Bali House Apartment Villa for Rent.
- 932346397598336 — Bali Villa Rental.
- 199650754695127 — Canggu, Krobokan, Seminyak, Prerenan Community Housing and Villa.
- 660644611267643 — Daily, monthly and yearly villa rentals in bali.
- 4182941211783475 — Bali Long Term Villa Rentals (ДРУГАЯ группа, не путать с 350448261731413).
- 602450786487008 — Bali Villas for rent & sell.

Тир 2 — ротацией, по несколько групп за слот, либо целиком раз в день утром:
- 2230099523983457 — Pererenan Community Housing — Rent Villa Room Seseh Cemagi Kedungu Tumbak.
- 2171026773145494 — Canggu Housing — Rent Room House Villa — Berawa Umalas Pererenan Kerobokan.
- 194681070865073 — Bali short & long term rentals.
- 350448261731413 — Bali Long Term Villa Rentals (первая из двух одноимённых).
- 682601905670900 — SESEH CEMAGI KEDUNGU VILLA & LAND / BUY, SELL, & RENT.
- 507714685968940 — Canggu Community Housing - Villas & Apartments.
- 179189038903917 — Bali Monthly Villa & Apartment Rentals.
- 80351 (canggucommunity80351) — Canggu Community — много шума, низкий приоритет.
- 1030073777014904 — Bali Seminyak Area Property And Accomodation — низкий приоритет.
- 1318808715514416 — canggu, seminyak, kerobokan, Bali community — низкий приоритет.
- https://www.facebook.com/groups/balirealestate/ — BALI ❤️ LONG TERM Yearly/Monthly Rentals.
- 137404790182506 — BALI VILLA FOR RENT YEARLY — в основном Bukit/Jimbaran, низкий приоритет.
- 710013955714323 — Bali House and Villa Rental — в основном листинги, низкий приоритет.

Сознательно НЕ включены (проверены, низкая ценность): balipropertyinfo, 168430473340996, обе Bali Expat Community (1459327810945814 и baliexpat), kerobokanVillas, 291069760977392, MatahariIslandProperty, Bali.Listings, 647190022760182, cheapvillarentalbali, 167049704070014, canggucommunityislove.

Помимо групп часть лидов приходит из Marketplace, прямых входящих и формы на сайте — Marketplace входит в обязательный чек-лист гейта.

Важно: многие авторы кросспостят один и тот же запрос сразу в несколько групп (3-5 одновременно). При выборе кандидата в Шаге 3 — это один и тот же человек, а не несколько лидов; дедуп по имени/номеру должен это отловить.

## Шаг 3. Фильтр и выбор одного кандидата

**Район здесь НЕ ранжируется.** Не применять ранжирование районов из скилла `bali-demand-matrix` — оно относится к другой задаче.

Берём: аренда виллы/дома от месяца и дольше, район из наших — Чангу (Батубулонг, Берава, Паданг Линьонг, Тибубенеанг, Падонан, Ума Булух, Бабакан), Переренан, Умалас, Керобокан, Семиньяк, Семер, Тумбак-Баюх, Мунггу, Сесех, Чемаги, Далунг.

Отбрасываем: краткосрок до месяца; комнаты, kos, kontrakan, шеринг, guest house room; районы Букит, Улувату, Санур, Убуд, Денпасар, Табанан, Кедунгу; посты агентов и брокеров (в т.ч. «ищу виллу для клиента», списки клиентских запросов, коммерческая аренда, покупка земли); запросы, где человек явно отказывается от наших районов.

**Бюджетная отсечка: от 30 млн IDR/месяц и выше.** Жёсткий порог квалификации. Действует и на исходящее, и **на заведение карточки тоже**. Кандидат ниже порога карточкой не становится — только бэклог с пометкой «manual review — low budget», без дожима. Если бюджет в посте не указан явно — не отбрасывать сразу, но при наличии альтернативы с явным бюджетом 30М+ выбирать её.

**Правило диапазонов.** Бюджет кандидата — это ОДНО число: нижняя граница заявленного диапазона. Проходит только кандидат, у которого ВЕСЬ диапазон от 30М и выше. «25–33М/мес» = 25М → НЕ проходит. «30–40М» → проходит. «До 35М» без нижней границы → считать «не указан явно». Годовой бюджет делить на 12 и применять то же правило: 300M/год = 25M/мес → нет; 360M/год = 30M/мес → да. **Любая неоднозначность бюджета трактуется ПРОТИВ отправки исходящего.**

**РАНЖИРОВАНИЕ ПО ЦЕННОСТИ СДЕЛКИ — главный критерий выбора.** Среди прошедших отсечку выбирать по `бюджет/мес (нижняя граница) × срок в месяцах` (год и дольше = 12, 6+ мес = 6, 3+ мес = 3, 1–2 мес = 2, срок не указан = 3). Долгосрочник с высоким чеком ВСЕГДА приоритетнее краткосрочника со свежим постом. Свежесть — тайбрейкер при сопоставимой ценности (в пределах ~1.5×), а не самостоятельный критерий.

**Окно свежести — двухуровневое.**
- Обычные кандидаты: 24–48 часов.
- Кандидаты высокой ценности (срок 6+ месяцев И бюджет 40М+/мес, либо ценность от ~300М): окно 14 дней. Таким запросам подходит мало объектов, люди с высоким чеком ищут неделями, а цена упущенной сделки на порядок выше цены одного сообщения из квоты.

**Механический гейт перед отправкой — обязательный шаг.** Непосредственно перед отправкой любого исходящего выписать одной строкой: `ГЕЙТ: бюджет = <нижняя граница>М/мес; ≥30 — да/нет; ценность = <бюджет × срок>М; свежесть в пределах окна класса (48ч обычный / 14д высокая ценность) — да/нет; в бэклоге нет кандидата с большей ценностью в своём окне — да/нет`. Все «да» — можно отправлять. Хотя бы одно «нет» — исходящее запрещено. Строка обязана попадать в отчёт дословно.

**Свежесть у сильного кандидата — не повод спускаться ниже по ценности.** Если у лучшего по ценности кандидата не удаётся подтвердить дату: дожать проверку — целевой поиск по уникальной фразе, пермалинк, кросспосты, JS-фильтр меток времени. Если дата не подтверждена — пропустить исходящее и оставить кандидата ПЕРВЫМ в бэклоге с пометкой «дата не подтверждена». Выбирать вместо него кандидата с меньшей ценностью запрещено.

Кандидатов ранжировать по ценности и писать **только лучшему** среди прошедших гейт. Остальных — в бэклог.

## Шаг 4. Контакт и карточка

Если WhatsApp указан прямо в посте — карточку заводить сразу, квота не тратится. **Это основной путь выполнения закона №1**, а не побочный улов: такие кандидаты ищутся целенаправленно в каждом прогоне.

Если «DM me» — открыть https://www.facebook.com/messages/t/<user_id>, нажать Continue, написать личное сообщение и попросить WhatsApp. Это тратит квоту. Карточка заводится ПОСЛЕ ответа с номером.

Стиль сообщения: обращение по имени, «this is Nick from Unicorn Property», пересказ его требований своими словами, одна честная зацепка по существу, просьба дать WhatsApp. Не длиннее 600 знаков. Без эмодзи, без рекламных штампов, не выдумывать объекты, которых нет в каталоге.

Дедуп перед сообщением и перед карточкой:
- открыть тред в Messenger — если наше сообщение там уже есть, повторно не писать;
- `GET /api/v4/leads?query=<Имя>&limit=50`
- `GET /api/v4/contacts?query=<номер>&with=leads` — сверять по последним 9 цифрам номера;
- дешёвый способ получить весь дедуп-сет одним запросом: `GET /api/v4/leads?query=FB%20Lead&limit=100&order[created_at]=desc` и сравнить имена.
- Если человек уже в CRM с активным лидом, но снова постит — НЕ заводить дубль, а добавить в активный лид английскую заметку «Still actively searching - posted again <дата> in <группа>» с новыми деталями запроса.

Создание карточки (amoCRM):
- `POST /api/v4/leads` — name `FB Lead: <Имя автора>`, `pipeline_id` 11119150, `status_id` 87301078, `responsible_user_id` 13372414, тег `Amelia`
- контакт с номером WhatsApp
- одна заметка `POST /api/v4/leads/notes`, `note_type: "common"`, ТОЛЬКО на английском, одной строкой:
  `Type: RENTAL. Source: <название группы + ID + свежесть поста, либо Messenger inbound>. Request: <спальни, район, сроки, требования>. Budget: <сумма или not specified>. WhatsApp: <номер>`

**КРИТИЧНО: бот Amelia автоматически закрывает «пустые» карточки как Closed - lost.** Если передать только name/pipeline_id/status_id/responsible_user_id/тег — карточка через 10-30 секунд переводится в `status_id` 143 и пропадает из канбана. Поэтому при создании (или сразу вторым PATCH) обязательно передавать `custom_fields_values`:
  - 956449 «Budget» (text)
  - 959041 «Number of Bedrooms» (text)
  - 959039 «Preferred Area or District» (text)
  - 968367 «Move-in Timeline» (text)
  - 968489 «Request: bedrooms» (numeric)
  - 968491 «Request: area» (text)
  - 968497 «Request: budget (jt/mo)» (numeric, в миллионах IDR)
  Если данных нет — заполнять лучшим приближением, но не оставлять пустыми.

**Обязательная проверка после создания.** Подождать ~35 секунд и запросить `GET /api/v4/leads/<lead_id>` — если `status_id` стал `143`, дозаполнить custom_fields_values и `PATCH` status_id обратно на 87301078, затем перепроверить ещё раз через ~35 сек. Это обязательный шаг для каждой новой карточки.

**Обязательная проверка связки лид↔контакт.** `POST /api/v4/contacts` с `_embedded.leads` не всегда реально линкует контакт к сделке. После создания контакта всегда проверять `GET /api/v4/leads/<lead_id>/links` — должен быть `{"to_entity_type":"contacts","to_entity_id":<contact_id>}`. Именно `/links`, а не `?with=contacts`. Если пусто — чинить: `POST /api/v4/leads/<lead_id>/link` с телом `[{"to_entity_id": <contact_id>, "to_entity_type": "contacts"}]`, затем перепроверить.

Только после подтверждённой связки И подтверждённого удержания status_id 87301078 карточка считается заведённой и закрывает закон №1.

Всё, что попадает в amoCRM, — только на английском. Служебных комментариев на русском в карточки не писать: их читает бот Amelia и принимает за реплику клиента.

## Шаг 5. Отчёт

Формат отчёта задаётся файлом `fb-rental-demand-scout-gate` и подчиняется Правилу единственного результата. Здесь только содержание:

- номер слота;
- чек-лист из 11 источников с цифрами по каждой строке (`кандидатов N / с номером M / новых K`) и перечень источников, добавленных сверх списка;
- состояние квоты в указанном формате и результат живой проверки лимита;
- кому написали + дословная строка ГЕЙТ из Шага 3, либо явное «исходящее не отправлено» с причиной;
- какие карточки завели, с ID сделок и контактов;
- **таблица всех номеров WhatsApp, встреченных за прогон**: номер, имя, исход (`заведён lead_id <id>` / `дубль, лид <id>` / `отклонён: <одна из четырёх причин>`);
- бэклог и лучший кандидат в очереди;
- обязательная само-проверка последней строкой по форме из гейта.

Отчёт пишется только при наличии `lead_id` либо при внешнем отказе с артефактом. Прогон без того и другого не отчитывается, а продолжается.

## Бэклог

Бэклог живёт от прогона к прогону и обновляется каждый слот. Правила ведения: сортировка по ценности сделки, у каждой записи фиксируются имя, user_id или группа, дата поста, спальни, район, срок, бюджет, способ связи и пометка о состоянии (например «дата не подтверждена», «исчерпан: DM + follow-up», «manual review — low budget»).

Записи старше 14 дней выпадают даже из расширенного окна высокой ценности и переносятся в архивную часть — их можно использовать только если у человека нашёлся более свежий кросспост.

